services:
  node:
    image: node:20-slim
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && npm run compile 2>&1"

  python:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "pip install ./python --quiet 2>&1"
